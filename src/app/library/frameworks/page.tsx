import Link from "next/link";
import { PageIntro } from "@/components/page-intro";
import { frameworks } from "@/data/frameworks";

export default function FrameworksPage() { return <main className="mx-auto max-w-6xl px-6 sm:px-10"><PageIntro eyebrow="Investment framework library" title="A repeatable way to think." description="A structured collection of source-led frameworks for turning qualitative questions into a disciplined research process." /><section className="py-10 sm:py-14">{frameworks.map((framework, index) => <article key={framework.slug} className="grid gap-4 border-t border-line py-8 sm:grid-cols-[8rem_minmax(0,1fr)]"><p className="text-xs font-semibold tracking-[0.14em] text-accent uppercase">Framework {String(index + 1).padStart(2, "0")}</p><div><div className="flex flex-wrap items-center gap-x-3 gap-y-2 text-xs font-semibold tracking-[0.12em] text-muted uppercase"><span>{framework.category}</span><span aria-hidden="true" className="text-line">/</span><span>{framework.status}</span><span aria-hidden="true" className="text-line">/</span><span>{framework.readingTime}</span></div><h2 className="mt-4 font-display text-3xl tracking-[-0.03em]"><Link className="transition-colors hover:text-accent focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent" href={`/library/frameworks/${framework.slug}`}>{framework.title}</Link></h2><p className="mt-3 max-w-2xl text-sm leading-6 text-muted">{framework.purpose}</p><Link href={`/library/frameworks/${framework.slug}`} className="mt-5 inline-block text-sm font-semibold transition-colors hover:text-accent">View framework &rarr;</Link></div></article>)}</section></main>; }

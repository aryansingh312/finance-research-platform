import { PageIntro } from "@/components/page-intro";
import { frameworks } from "@/data/library";

export default function FrameworksPage() { return <main className="mx-auto max-w-6xl px-6 sm:px-10"><PageIntro eyebrow="Investment framework library" title="A repeatable way to think." description="Original frameworks for turning qualitative questions about a company or industry into a disciplined research process." /><section className="py-10 sm:py-14">{frameworks.map((framework, index) => <article key={framework} className="grid gap-4 border-t border-line py-8 sm:grid-cols-[8rem_minmax(0,1fr)]"><p className="text-xs font-semibold tracking-[0.14em] text-accent uppercase">Framework {String(index + 1).padStart(2, "0")}</p><div><h2 className="font-display text-3xl tracking-[-0.03em]">{framework}</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-muted">A practical framework will define the key questions, show how to apply them, and connect them to an investment conclusion.</p></div></article>)}</section></main>; }

import { PageIntro } from "@/components/page-intro";
import { knowledgeTopics } from "@/data/library";

export default function KnowledgePage() { return <main className="mx-auto max-w-6xl px-6 sm:px-10"><PageIntro eyebrow="Finance knowledge base" title="The fundamentals of value." description="Clear explanations of the concepts used to analyze businesses, allocate capital, and build a long-term investment process." /><section className="grid grid-cols-1 border-t border-line py-10 sm:grid-cols-2 lg:grid-cols-3">{knowledgeTopics.map((topic, index) => <article key={topic} className="border-b border-r border-line px-5 py-6"><p className="text-xs font-semibold tracking-[0.14em] text-accent uppercase">Topic {String(index + 1).padStart(2, "0")}</p><h2 className="mt-3 font-display text-2xl tracking-[-0.025em]">{topic}</h2><p className="mt-4 text-sm leading-6 text-muted">An explainer will cover the definition, why it matters, practical application, and related concepts.</p></article>)}</section></main>; }
